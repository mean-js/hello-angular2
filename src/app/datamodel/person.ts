export class Person {
    public pname: string;
    public pid: number;
    public parea: string;

    constructor(pname, pid, parea) {
        this.pname = pname;
        this.pid = pid;
        this.parea = parea;
    }
}
