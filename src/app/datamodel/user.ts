export class User {
    public uname: string;
    public uid: number;
    public uarea: string;

    constructor(uname, uid, uarea) {
        this.uname = uname;
        this.uid = uid;
        this.uarea = uarea;
    }
}
