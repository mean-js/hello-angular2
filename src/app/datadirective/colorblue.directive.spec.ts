import { ColorblueDirective } from './colorblue.directive';

describe('ColorblueDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorblueDirective();
    expect(directive).toBeTruthy();
  });
});
